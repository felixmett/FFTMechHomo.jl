To build documentation, run `scripts/builddocs.sh` from top directory of FFTMechHomo.jl

To build demo scripts from all files in docs/literate into /demo, run `scripts/builddemofiles.sh` from top directory of FFTMechHomo.jl